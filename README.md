# TextSummarizer
A small python project for intern which can summarize text and analyse the sentiment of the text.

# Requirements:
```
pip install scikit-learn textblob matplotlib nltk
python -m textblob.download_corpora
```

# Run:
```
python main.py
```

# Note:
To text to be summarized are stored in the 'text.txt' file.